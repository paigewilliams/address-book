module Personal
  class Contact
    # attr_accessor(:name_first, :name_last, :address)

    def initialize(first_name, last_name)

      @name_first = first_name,
      @name_last = last_name
    end
  end
end
